# Task Index

> *One-line summary per closed task. Maintained by Meridian (with Codex's help).*

---

## Open Tasks

(none yet under the `core/` system)

---

## Closed Tasks

| Date | Slug | Summary |
|---|---|---|
| 2026-06-22 | (pre-core) tabs-persistent | Added shared AdminTabs component mounted on every /admin/* page; member count badge live (168). Shipped before `core/` existed; retroactively logged. |

---

## Pre-Core History

Tasks completed before the `core/` system was created on 2026-06-22 are recorded in `/home/z/my-project/worklog.md`. They include:

- v2.0 backup audit
- Email campaign system
- Production error boundary restoration
- Photos tab main-image selector (lost 3 times — restored each time)
- Speaker management tab
- Registrants tab with bulk upload
- Event creation tab with co-host picker
- Co-host role (EventCoHost junction table)
- Persistent admin tab bar

Going forward, every task gets its own folder under `core/tasks/<YYYY-MM-DD>-<slug>/` and a row in this index.
