# dependencies
skills/
node_modules/

# next.js build output
.next/
out/
build/
dist/

# vercel
.vercel

# env files (do NOT commit secrets)
.env
.env.*
!.env.example

# Google Drive OAuth token (contains refresh_token — never commit)
scripts/.gdrive-token.json

# Local backup artifacts (large tarballs + manifests — keep local + Drive,
# never push to git)
download/aisalon-massapro-V*.tar.gz
download/aisalon-massapro-V*-MANIFEST.txt
download/v*-backup/

# typescript
tsconfig.tsbuildinfo
*.tsbuildinfo

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# IDE
.idea/
.vscode/
*.swp
*.swo
ai-salon-shareable/
dev.log
scripts/get-prod-db-url.mjs
scripts/patch-salon-paths.py
/tmp/prod_db_url.txt
old-deployment/

# Dev/backup directories — not deployed to Vercel
scripts/
download/
tool-results/
agents/
agent-ctx/
backups/
backups-email-queue/
*.tar.gz
v517

# Local SQLite db + pid files
db/
*.pid
*.pid.lock
*.log
set-test-pw.ts
aisalon-massapro-V*-MANIFEST.txt
CORE-PRACTICE.md

# Google Drive / GCP credentials (root-level — added 2026-07-17)
/.gdrive-token.json
/.gcp-service-account.json
